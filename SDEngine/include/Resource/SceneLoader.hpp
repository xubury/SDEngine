#ifndef SD_SCENE_LOADER_HPP
#define SD_SCENE_LOADER_HPP

#include "Resource/Export.hpp"

namespace SD {

class SD_RESOURCE_API SceneLoader {
   public:
    // Ref<Scene> LoadFromFile(const std::string &path);
    // void WriteToFile(const Scene &scene, const std::string &path);
};

}  // namespace SD

#endif  // !SD_SCENE_LOADER_HPP
